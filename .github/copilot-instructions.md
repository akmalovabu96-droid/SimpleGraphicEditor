# Canvas Foundry

This is a dependency-free vanilla HTML/CSS/JavaScript Canvas editor. Keep interactions in `app.js`, visual styling in `styles.css`, and preserve the Command-pattern history for drawing mutations.
